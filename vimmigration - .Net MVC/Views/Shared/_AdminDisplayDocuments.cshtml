@foreach (var doc in ViewBag.Documents as List<VeraCityImmigration.Models.DocumentViewModel>)
{
    <div class="col-lg-12">
        <a class=" bg-success pad-5" href='@Url.Action("RenderDocument", "Admin", new {id = doc.DocumentId})'>@Html.DisplayFor(modeldoc => doc.DocumentName)</a>
    </div>
    <div class=" col-lg-6 marginbot-10">
        @*@using (Html.BeginForm("DeleteDocument", "Account", new { id = doc.DocumentId }))
        {
            <input type="submit" class="del_btn" value="Delete" onclick="return confirm('Are you sure you want to delete @doc.DocumentName')" />
        }*@
        </div>
}
